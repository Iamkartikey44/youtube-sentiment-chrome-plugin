from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A lightweight Chrome extension that extracts YouTube comments in real time and applies NLP-based sentiment analysis to display sentiment insights directly on the YouTube interface.',
    author='Kartikey',
    license='',
)
